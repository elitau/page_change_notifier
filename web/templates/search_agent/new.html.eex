<h2>New search agent</h2>

<%= render "form.html", changeset: @changeset,
                        action: search_agent_path(@conn, :create) %>

<%= link "Back", to: search_agent_path(@conn, :index) %>
