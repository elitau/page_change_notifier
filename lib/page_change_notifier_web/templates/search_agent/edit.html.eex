<h2>Edit search agent</h2>

<%= render "form.html", changeset: @changeset,
                        action: search_agent_path(@conn, :update, @search_agent) %>

<%= link "Back", to: search_agent_path(@conn, :index) %>
